CREATE DATABASE sql_practice;
USE sql_practice;

CREATE TABLE employees (
    emp_id INT PRIMARY KEY,
    name VARCHAR(50),
    department VARCHAR(50),
    salary INT
);

INSERT INTO employees VALUES
(1, 'Ankita', 'Analytics', 50000),
(2, 'John', 'HR', 40000),
(3, 'Emma', 'Finance', 60000);

SELECT * FROM employees;
